# PCO Legislation API key (https://api.legislation.govt.nz)
NZ_LEGISLATION_API_KEY=your-pco-api-key-here

# HTTP Basic Auth credentials gating access to the MCP endpoint.
# Generate a strong password, e.g. `openssl rand -base64 24`.
MCP_USER=changeme
MCP_PASS=changeme
